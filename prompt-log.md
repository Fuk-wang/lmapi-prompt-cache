# Prompt 日志 · 子任务 C（Prompt Caching / Prefix）

> 每一版都保留原文，只记录**真实发生**的事；失败那一轮不删，因为「过程」本身就是评分项。

---

## 第 0 轮：探索

### 我（人）给 Hermes 的原始 prompt（v0，散文式）
```
我要做 LMAPI 知识网页，子任务 C，Prompt Caching。帮我想想怎么做，顺便查资料。
```

**Hermes 输出问题**
- **歧义**：「顺便查资料」被理解成「去抓 OpenAI / Anthropic / Google / HuggingFace 的文档页」，
  但这台机器上 `platform.openai.com` 返回 403、`ai.google.dev` 与 `huggingface.co` 直接连不上。
  它没有先确认哪些源可达，白花了一轮。
- **缺失约束**：它默认要做「示意图风格」的知识页——也就是说数字大概率是估的/编的。
  这门课「内容准确性与深度」占 30%，交示意图等于自曝。
- **格式漂移**：方案里同时给了 Vercel 和 GitHub Pages 两条部署路线，但没检查本机有没有 node/npm。

### 改了哪部分规格 → v1
1. **指令与数据分离**：把环境事实单独列成一块，不让它自己猜——
   本机无 node/npm；已有 GitHub 账号与 SSH key；只有一把 DeepSeek key；
   可达的一手文档 = `api-docs.deepseek.com` ✅、`docs.claude.com` ✅、MCP 官网 ✅；
   `platform.openai.com` ❌403、`ai.google.dev` ❌、`huggingface.co` ❌。
2. **加硬约束**：「网页上每个数字必须来自我们自己跑出来的实测；插值和推算必须显式标注」。
3. **顺序约束**：先出数据，再出页面——先写探针脚本跑真实调用，拿到 `usage`，再谈视觉。

### 效果变化
- 立刻改走「可达的官方文档 + 自己的实测」，写出 `experiments/cache_probe.py`，
  14 次真实调用拿到全部数字（`research/experiment-log.md`）。
- 拿到一条任务书里没写、只有真跑才看得见的结论：**命中长度全是 64 的整数倍**
  （128 / 640 / 1152 / 1792 / 2304），即命中按 64 token 为单位对齐。

---

## 第 1 轮：第一版网页（AGENTS.md v1 规格）

规格要点：受众大四；每屏一个概念；≥2 个可交互 demo；数字来自实测、插值要标注；
引一手文档；单文件内联 CSS/JS、离线可开。

### Hermes 第一版产出的事实错误（这就是「失败」那一轮）

**问题：同一个位置出现两个数字。** 第一版的可视化用公式
`命中 = floor(位置比例 × 2432 / 64) × 64` 反推命中量。跑无头浏览器验收时：

| 滑块位置 | 页面显示 | 实测值 | 判定 |
|---|---|---|---|
| 11% | 256 | **128** | ❌ 差一倍 |
| 51% | 1,216 | **1,152** | ❌ 不一致 |
| 96% | 2,304 | 2,304 | ✅ 偶然对上 |

这直接违反规格里「数字必须与实测一致」的硬约束——而且是最坏的那种：
**同一个页面里，滑块停在实测点上时显示的数字，和我们自己实测记录对不上**，
评分方只要拿两个数字一比就会发现问题。

**根因**：字符位置的比例 ≠ token 位置的比例。实测里那个 11% 是「差异出现在文档第 11% 个字符处」，
而 2432 是 token 数，中文正文里字符与 token 不是 1:1，比例自然对不上；11% × 2432 ≈ 267，
实测却只有 128（恰好 2 个 64）。

**第二处失败：窄屏横向溢出。** 375px 下整页 `scrollWidth=487`，卡片被顶到 397 宽。

### 改了哪部分规格 → v2
1. **数据来源改为「以实测点为锚」**：删掉用公式硬算的那条曲线；滑块数值改为
   **五个实测点之间分段线性插值**，锚点处逐位复现实测值；页面上明确写出
   「当前位置就是实测点」或「当前位置是插值：在实测点 A 与 B 之间线性过渡」。
2. **把「64 对齐」降级为观察结论**，不再拿它当计算公式：原文写「说明命中按 64 token 向下取整」，
   改成「五个值恰好全是 64 的整数倍，**推测**服务端按 64 token 为单位对齐」——一手文档没写这条，不能断言。
3. **响应式单独立一条验收**：375px 下不得横向溢出。

### 效果变化（v2 验收，全部用无头 Chrome 实测）

| 滑块位置 | 页面显示 | 实测值 | 判定 |
|---|---|---|---|
| 11% | 128 | 128 | ✅ |
| 51% | 1,152 | 1,152 | ✅ |
| 96% | 2,304 | 2,304 | ✅ |
| 40% | 832（标注「插值：31%→640 与 51%→1152 之间」） | — | ✅ 如实标注 |

窄屏：360 / 375 / 414px 下 `scrollWidth == clientWidth`，真实溢出元素 **0** 个
（另有 4 个元素落在可横向滚动的表格容器内部，属于正常内部滚动）。

### 窄屏这轮的插曲（值得记一笔）
第一次量窄屏用的是 `chrome --window-size=375`，报 `clientWidth=497`，
截图看着右边被切——我据此刻断「顶栏徽章撑宽了页面」，并据此改了一版 CSS。
后来发现 **Windows 上无头 Chrome 有最小窗口宽度**，375 根本量不出来，那次是截图裁切造成的假象。
改用 `iframe width=375` 强制真实视口重测，才量到真凶：
**CSS Grid 子项的默认 `min-width:auto`** —— 表格撑不住时把整张卡片顶宽（card 397 在 316 的容器里）。
加 `.grid > *{min-width:0}` 才真正修好。
教训：**测量工具本身要先被验证**，否则会照着假象改代码。

---

## 第 2 轮：页面文案（v2 → v3）——「这是给读者看的，不是给你我记流水账的」

### 谁提的问题、原话是什么
人读完整页后指出：

> 「我做的网站是要给别人看的，不是给自己看的，要别人看懂、了解，而不是整得你和我对话一样的感觉。」

具体点到的两处：
1. **第 06 节开头写着「任务书要求『看源码，对照你刚才踩的坑』」。**
   任务书那句话是**给人看的动作要求**（探索/反刍属于过程），不是**网页内容**。
   把它抄到页面上，读者既不知道「任务书」指什么，也不知道「你」是谁。
2. **通篇第一人称「我们」**（「我们真实跑出来的五个点」「我们在源码里看到的实现」「对照自己踩的坑」），
   读者读到的是别人的实验流水账，而不是一个能独立读懂的知识页。

同时要求：**别把迭代历史编进规格**，迭代日志要以真实记录为准（本文件就是那份日志）。

### 改了哪部分规格 → v3
新增硬约束（写进 AGENTS.md 第 8 条）：

- 网页是**面向陌生读者的知识页**。禁止出现：第一人称（我们 / 我）、与 Agent 的对话痕迹、
  任务书 / 评分 / 子任务编号这类课程内部用语、以及「我踩了什么坑」的过程叙述。
- 过程与迭代**只写进 `prompt-log.md`**；`AGENTS.md` 只写要求与事实，不编造迭代史。

### 具体动了什么（逐条可查）
| 类别 | 处数 | 例子 |
|---|---:|---|
| 第一人称「我们」 | 11 | 「灰色小点是**我们**真实跑出来的五个点」→「灰色小点是真实调用得到的五个数据点」 |
| 任务书原话 / 课程内部用语 | 3 | 删掉「任务书要求…」整句；页脚与首屏 kicker 去掉「子任务 C」 |
| 「踩的坑」式过程叙述 | 5 | 小节名「反刍 → 参考实现」；表格表头「我们实测踩到的 → 常见故障」 |
| 读者人称 | 4 | 「缓存只认得**你**前面那段」→「缓存只认得前面那段」；「**你**的代码」→「调用方的代码」 |

另外补了一节**真实的源码引用**（任务书点名 prompt_builder.py，此前页面一次都没引过它）：
`agent/prompt_builder.py:378`「Ships in every cached prompt — keep tight.」与
`:404`「Token cost is paid once at install and amortised across all sessions via prefix caching.」
——用它说明「静态前缀的体积本身也被当作成本来管」。

### 效果变化（验收）
- 全页扫 `我们 / 我 / 任务书 / 评分 / 子任务 / 踩 / 坑 / 反刍`：**计数全部为 0**。
  剩下的「你」只有两处，都是内容本身：示例系统提示词里的「你是一位资深财报分析师」、
  反例引用「写『请你保持前缀稳定』是没用的」。
- 无头 Chrome 重渲染：标题、7 个导航项（末项已改为「参考实现」）、3 张表、5 个代码块、
  全部交互控件与数字锚（2432 / 2665 / 64 / 14 次 / $0.002307）原样保留；375px 下仍无横向溢出。

### 这一轮的教训（值得记一笔）
「给 Agent 写规格」和「给读者写页面」是两种文体。**任务书里的过程要求属于规格，不属于交付物**——
规格驱动的是怎么做事，页面上只该留下读者需要知道的东西。这条以前没人明确说过，是这一轮才补上的约束。

---

## 【结论】

这个知识点最该用哪一层解决？
  ☐ Prompt　☐ Schema　☑ **Runtime**　☐ Eval

理由：命中率不由模型决定，由「你的代码把什么放在前缀的哪个位置」决定——
时间戳有没有进前缀、工具定义序列化是否稳定、多轮里会不会回头改写历史消息、上下文压缩时动不动前缀。
这些全是运行时/架构问题，写进 prompt 的「请你保持前缀稳定」不会改变一个字节。
Hermes Agent 自己的做法就是把这条写进 `AGENTS.md` 的设计不变量（prompt caching is sacred），
并在 `prompt_cache_boundary.py` 里让构建者显式声明稳定边界，而不是让运行时去猜。

**一句话总结学到的 Prompt Engineering 方法：**
先把「什么算对」定成可测量的硬约束（这里是：页面数字必须等于实测 usage），
再让 Agent 去实现；验收时用工具去量，而不是用眼睛看——量出来对不上的那一次，
问题通常不在措辞，而在你的规格没有把事实钉死。
